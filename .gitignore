# Vim
*~
*.sw[p_]

# Sublime Text
*.sublime-project
*.sublime-workspace

# Ruby Gem
*.gem
.bundle
Gemfile.lock
**/vendor/bundle

# Node.js and NPM
node_modules
npm-debug.log*
codekit-config.json

# Astro
.astro
dist

# Files moved out of public/uploads/ by scripts/frontmatter/11-archive-orphans.mjs
# (uploads not referenced from any built HTML page — kept out of git, but on disk
# so you can review/restore before deleting permanently).
_archived-uploads/

# macOS
.DS_Store

# Jekyll generated files
.jekyll-cache
.jekyll-metadata
.sass-cache
_asset_bundler_cache
_site

# Editor
docs/
test/
.vs/

_DesignSystem